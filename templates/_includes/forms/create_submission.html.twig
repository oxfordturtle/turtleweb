{# twigs: form #}

<div class="form">
  {{ form_start(form) }}
    <fieldset>
      <div class="form-legend">File to Upload</div>
      <div class="form-inputs">
      {{ form_row(form.file) }}
      </div>
    </fieldset>
    <div class="form-buttons">
      <button type="submit" class="green-button">Upload</button>
    </div>
  {{ form_end(form) }}
</div>
